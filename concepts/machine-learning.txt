# 📚 Machine Learning: Fundamentos e Conceitos

Machine Learning (ML) é um campo da Inteligência Artificial (IA) que permite que máquinas **aprendam a partir de dados**
 sem serem explicitamente programadas para cada tarefa.

Em vez de escrever passo a passo o que deve ser feito, você **fornece exemplos**, e o modelo **aprende sozinho a identificar padrões**.

---

## 🧠 Como funciona?

1. **Coleta de dados**
2. **Treina o modelo com os dados**
3. **Testa com novos dados**

---

## 🧪 Tipos de Aprendizado

### 🔷 Supervisionado

Você fornece **dados com rótulos (labels)**. O modelo aprende a prever os rótulos com base nas entradas.

#### 🔸 Tarefas comuns:

- **Classificação**: prever classes discretas.
  - Ex: Gato vs. Cachorro, Tipos de frutas
  - Pode ser **binária** (2 classes) ou **multiclasse**
  
- **Regressão**: prever valores contínuos (números reais).
  - Ex: Temperatura, Preço de uma casa

---

### 🔷 Não Supervisionado

Você fornece **dados sem rótulos**, e o modelo tenta **descobrir padrões ou agrupamentos** por conta própria.
Unsupervised Clustering -> Assume grupos ao mundo.
A representação do mundo pode ser a função.
Com imagens: Representation learning.

Mais complexo; analisa em tempo real; precisão moderada.
Anomaly Detection: Acha casos que fogem muito aos grupos padrões.

Partitional Clustering: Divide os dados em conjuntos que não se sobrepoem. Partições.
  - Rápido.
  - Fácil de interpretar.
  - Precisa definir K antes.
  - Sem hierarquia
  - Busca minimizar alguma função de custo, geralmente relacionada à distância dos pontos
  aos centro do cluster.

  Algoritmos:
  K-means -> Escolhe K, o número de clusters. Inicia K centróides (Números que representam
  cada cluster). atribuição de pontos aos clusters cujo centróide está mais próximo. Recalcula
  os centróides com a média de todos os pontos. Repete.
  - Os centróides podem ser alguns próprios pontos. Ponto médio de um cluster. Não é necessariamente
  um ponto do cluster.

Hierarchical Clustering: Uma árvore que tem um conjunto de agrupamentos.
  - É necessário calcular a distância entre clusters para encontrar os mais proximos e agrupá-los.
  - Utiliza-se uma matriz de distâncias.
  - A distância entre pontos pode ser Euclidiana, manhattan...
  - Distância entre Clusters: Como vai ser medida a distãncia...
  Single linkage - Distância minima entre dois pontos dos clusters. Complete linkage - Distân-
  cia máxima. Average linkage - Distancia média entre todos os pontos...

  - O(n^3) - Custoso.
  - Não precisa definir o número de clusters inicialmente.

  Aglomerativo: Mais comum. Começa cada um sendo seu proprio conjunto. Os agrupamentos
  mais proximos são fundidos. Árvore chamada de dendrograma. Abordagem bottom-up.

  Divisivo: Todos os pontos começam em um único agrupamento e são divididos. Menos usado
  por ser mais custoso.
---

### 🔷 Aprendizado por Reforço

O modelo aprende com **tentativa e erro**, recebendo **recompensas** ou **penalidades** com base nas decisões que toma.

---

## ⚙️ O que é um Modelo?

Um **modelo** é uma função que:

- Recebe entradas (features)
- Processa essas entradas
- Retorna uma saída (previsão)

Durante o **aprendizado**, o modelo:

1. Recebe dados de entrada
2. Faz uma previsão
3. Compara com o valor real
4. Ajusta seus parâmetros para melhorar com base na **função de perda (loss)**

---

## ❌ Funções de Perda (Loss Functions)

A função de perda mede o **erro da previsão** comparado ao valor verdadeiro.

- **L1 Loss (Erro Absoluto Médio)**:
Loss = sum(| y_real - y_previsto |)

- **L2 Loss (Erro Quadrático Médio)**:
Loss = sum(( y_real - y_previsto )²)
---

## ✅ Métricas

- **Accuracy (Precisão)**: porcentagem de previsões corretas (muito usada em classificação)

---

## 🔁 Divisão dos Dados

- **Treinamento**: usado para ajustar o modelo (tem feedback do loss)
- **Validação**: avalia o modelo em dados **nunca vistos** durante o treino (sem feedback)
- **Teste**: avaliação final, simula o uso real do modelo com dados totalmente novos

---

## 🧾 Tipos de Dados (Features)

### 📊 Qualitativos (Categóricos)

- Representam **categorias** distintas
- Ex: Gênero, Nacionalidade

#### ➤ One-hot Encoding:
Transforma categorias em vetores numéricos:
```text
Ex: Gênero (Masculino, Feminino)
→ [1, 0] ou [0, 1]
Nominais: sem ordem natural entre os valores (ex: cor dos olhos)

Ordinais: com ordem natural (ex: bebê < jovem < adulto)

🔢 Quantitativos (Numéricos)
Valores que representam quantidades

Podem ser:

-Discretos: número de filhos, andares
-Contínuos: temperatura, altura, distância